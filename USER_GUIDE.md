# 代理管理中心使用指南

> 本指南记录 0.3.10 的当前操作方式。插件加载时会自动将 AstrBot 的全局 HTTP、HTTPS、通用 SOCKS 代理配置指向插件入口，并检查 Provider、平台、插件和 MCP 的统一接入协议。默认未命中规则是内核 `DIRECT`；没有请求级证据时不会宣称任何组件已接管。最终目标及后续开发约束见[《最终产品定义与架构约束》](https://github.com/gobelieve0905/astrbot_plugin_proxy_manage/blob/develop/docs/PRODUCT_DEFINITION.md)。

桌面端使用左侧导航，移动端使用顶部横向导航。页面顶部的“恢复上一版”和“预览并保存”作用于规范化配置；内核的下载、启动、停止和应用操作位于“内核管理”，不会与普通配置保存混在同一操作组。

## 首次接入

1. 打开“内核管理”，选择“下载安装”。安装在后台运行，页面显示当前来源、字节进度、重试次数，并允许取消。
2. 全部受信来源超时或失败时，页面会进入“等待离线上传”。使用页面展示的官方地址在本地下载对应制品，核对文件名后上传；插件仍会验证固定 SHA-256。
3. 等待制品状态显示已校验、进程状态显示运行。控制地址、Secret、监听地址和代理入口均由插件内部管理，无需手工填写。
4. 导入一个或多个 HTTP/HTTPS 订阅，检查预览后确认导入。协议未验证的节点会保留，但不会写入运行配置。
5. 在节点页按订阅来源、协议、地区和状态筛选，手动测速后选择组成员。
6. 建立 `select`、`url-test` 或 `fallback` 代理组。自动组需要设置测速目标、周期、切换容差和全部不可用时的策略。
7. 建立规则组，填写名称、域名、精确/后缀匹配、优先级和目标代理组。优先级数字越小越先匹配。
8. 预览规则并应用配置。只有运行节点、组成员、实际选择和规则内容/顺序全部核对一致，状态才会显示“已应用”。
9. 使用实际出站验证接口请求返回 JSON `ip` 字段的受控 HTTPS 地址。页面分别展示入口、规则和出口证据；只有同一次请求的内核连接记录能够证明规则、代理组和实际节点链路，并取得出口 IP 时才确认出口，否则显示“无法判定”。

配置应用失败时，插件只恢复修订标识匹配的上一份已验证配置。首次应用或没有可信历史配置时，插件会写入 `MATCH,REJECT` 并核对，阻断未知流量；若失败关闭配置也无法核对，状态显示“恢复失败”，需要立即检查内核。

## AstrBot 接入范围

| 场景 | 接入方式 | 支持状态 |
| --- | --- | --- |
| AstrBot 核心及遵循全局 HTTP 代理的下载请求 | 持久化 `http_proxy`/`no_proxy`；插件启动后在 AstrBot 进程设置 HTTP、HTTPS 和 SOCKS 入口 | 0.3.10 已接入，需重启后做请求级验证 |
| 使用 AstrBot 公共 HTTP 客户端且继承核心代理配置的插件 | 使用核心 `http_proxy` | 尚未逐项接入和验证 |
| 支持独立代理字段的平台适配器 | 在适配器配置中填写统一 HTTP/SOCKS 入口 | 条件支持，应按适配器文档验证 |
| 支持代理或自定义 HTTP 客户端的模型提供商 | 在对应客户端配置代理 | 条件支持，应执行无业务凭据的受控请求验证 |
| 自建 SDK、原生 socket、显式 `trust_env=false` 的第三方插件 | 由该插件显式配置代理 | 不会自动接入 |
| 飞书、Telegram 等已运行平台连接 | 先在测试实例验证重连和长连接支持 | 不自动修改，避免中断现有连接 |

## MCP 与跨容器出口

| MCP 形态 | 接入方式 | 重启与限制 |
| --- | --- | --- |
| AstrBot 启动的 stdio MCP | 向子进程注入 `HTTP_PROXY`、`HTTPS_PROXY`，必要时注入 `ALL_PROXY`，指向 AstrBot 容器内回环入口 | 重启该 MCP 进程；当前版本只审计配置，不自动注入 |
| 同机独立进程 | 使用由管理员控制、只绑定宿主机 `127.0.0.1` 的转发入口 | 当前没有安全转发时显示未接入，不开放容器入口到宿主机 |
| 独立 MCP 容器 | 与 AstrBot 加入同一个明确受信的 Docker 私网，使用插件生成的带认证私网入口 | 重建或重启容器；不得用 `ports` 发布代理端口 |

私网入口使用独立随机用户名和密码，凭据保存在插件私有运行目录且权限为 `0600`，不复用内核控制密钥。管理页只显示认证是否就绪、端口和部署提示，不显示凭据。Docker 网络成员都能到达该端口，只有确实受信的容器才应加入该网络；代理认证用于阻止同网未配置凭据的进程使用出口。

## 统一接入协议

外部插件在自身插件目录放置 `proxy_manager_integration.json`，MCP 在自身配置项内放置 `proxy_manager` 对象。两者使用同一份公开、无凭据声明：

```json
{
  "protocol": "astrbot.proxy-manager/v1",
  "mode": "astrbot-environment",
  "protocols": ["http", "https", "websocket"],
  "restart": "process",
  "auto_apply": false
}
```

`mode` 只能是 `astrbot-environment`、`private-network` 或 `manual`。前者表示组件遵守 AstrBot 的代理环境；`private-network` 表示独立容器使用认证私网入口；`manual` 表示组件需由其所有者配置。声明只允许上述五个字段，不能包含 URL、用户名、密码、Token 或 Headers；多余字段会被标记为协议无效。插件在加载和之后每 30 秒检查新增或变化的声明，也可在概览点击“重新检查”。协议兼容只代表已纳入策略，仍需实际请求与内核记录才能确认出口。

概览中的动态流量清单使用四类状态：“已接管”必须有请求级证据；“明确直连”表示请求进入内核后由规则选择 `DIRECT`；“未接入”表示组件没有使用插件入口；“无法判定”表示只观察到配置或入口，证据不足。平台域名模板只生成内核规则，不会自动修改平台 SDK。

在“全局代理接入”点击接入后，页面会标记 AstrBot 需重启。插件停用、热重载或 AstrBot 关闭时，已写入的全局代理保持不变，自管内核会停止，因此新的外部请求应失败而不是直连。卸载前必须先点击“恢复旧配置”并重启 AstrBot；直接删除插件不会自动改写 AstrBot 配置。

## 支持矩阵

| 类别 | 已验证 | 保留但未验证 | 不支持 |
| --- | --- | --- | --- |
| 节点 URI | AnyTLS、HTTP、HTTPS、SOCKS5/SOCKS5H | SS、VMess、VLESS、Trojan、Hysteria2、TUIC | 未知协议明确标记原因 |
| 订阅格式 | 常见 URI 列表、适用的 Clash YAML | 供应商私有字段 | 任意脚本或可执行订阅 |
| 内核 | 插件自管 Mihomo 1.19.31、sing-box 1.14.1 | Xray 等后续适配器 | 外部共享或无法核对的内核 |
| 部署 | 插件私有目录、回环监听、随机密钥 | 离线制品安装 | 公网控制端口或用户指定外部服务 |
| 制品 | 官方固定 URL 与 SHA-256、离线上传同摘要 | 后续版本升级清单 | `latest`、第三方未校验镜像 |

无内核时管理页、订阅解析、节点整理、代理组和规则组编辑仍可使用。原生协议测速、自动组选优、应用配置及实际出口验证会显示缺少内核或统一代理入口的具体原因。

## 全部节点不可用

自动组不会自动加入 `DIRECT`。`fail-closed` 组在全部成员不可用时保持失败，不绕过代理；`keep-last` 由内核保留最后选择，但该节点仍可能无法连接。需要直连时应建立明确的直连规则。
