# 代理管理中心使用指南

> 本指南记录 0.3.2 的当前操作方式。插件已自管固定版本 Mihomo，但多内核和 AstrBot 全部实际出站流量接入仍未完成。最终目标及后续开发约束见[《最终产品定义与架构约束》](https://github.com/gobelieve0905/astrbot_plugin_proxy_manage/blob/develop/docs/PRODUCT_DEFINITION.md)。

## 首次接入

1. 打开“内核管理”，选择“下载安装”；无法访问 GitHub 时，下载页面所示固定版本的对应平台官方制品，通过离线入口上传。插件必须验证固定 SHA-256 后才会安装。
2. 等待制品状态显示已校验、进程状态显示运行。控制地址、Secret、监听地址和代理入口均由插件内部管理，无需手工填写。
3. 导入一个或多个 HTTP/HTTPS 订阅，检查预览后确认导入。协议未验证的节点会保留，但不会写入运行配置。
4. 在节点页按订阅来源、协议、地区和状态筛选，手动测速后选择组成员。
5. 建立 `select`、`url-test` 或 `fallback` 代理组。自动组需要设置测速目标、周期、切换容差和全部不可用时的策略。
6. 建立规则组，填写名称、域名、精确/后缀匹配、优先级和目标代理组。优先级数字越小越先匹配。
7. 预览规则并应用配置。只有运行节点、组成员、实际选择和规则内容/顺序全部核对一致，状态才会显示“已应用”。
8. 使用实际出站验证接口请求返回 JSON `ip` 字段的受控 HTTPS 地址。页面分别展示入口、规则和出口证据；只有出口 IP 与运行代理组实际选择均可取得时才确认出口，否则显示“无法判定”。

配置应用失败时，插件只恢复修订标识匹配的上一份已验证配置。首次应用或没有可信历史配置时，插件会写入 `MATCH,REJECT` 并核对，阻断未知流量；若失败关闭配置也无法核对，状态显示“恢复失败”，需要立即检查内核。

共享 Mihomo 实例目前只允许状态核对和节点测速。标准控制 API 无法提供含凭据的可信完整配置，插件无法可靠合并和恢复时会阻止写入。

## AstrBot 接入范围

| 场景 | 接入方式 | 支持状态 |
| --- | --- | --- |
| AstrBot 核心及遵循全局 HTTP 代理的下载请求 | AstrBot `http_proxy` 指向统一 HTTP 入口 | 支持，需重启 AstrBot后生效 |
| 使用 AstrBot 公共 HTTP 客户端且继承核心代理配置的插件 | 使用核心 `http_proxy` | 支持，取决于插件是否绕过环境或核心客户端 |
| 支持独立代理字段的平台适配器 | 在适配器配置中填写统一 HTTP/SOCKS 入口 | 条件支持，应按适配器文档验证 |
| 支持代理或自定义 HTTP 客户端的模型提供商 | 在对应客户端配置代理 | 条件支持，应执行无业务凭据的受控请求验证 |
| 自建 SDK、原生 socket、显式 `trust_env=false` 的第三方插件 | 由该插件显式配置代理 | 不会自动接入 |
| 飞书、Telegram 等已运行平台连接 | 先在测试实例验证重连和长连接支持 | 不自动修改，避免中断现有连接 |

## 支持矩阵

| 类别 | 已验证 | 保留但未验证 | 不支持 |
| --- | --- | --- | --- |
| 节点 URI | AnyTLS、HTTP、HTTPS、SOCKS5/SOCKS5H | SS、VMess、VLESS、Trojan、Hysteria2、TUIC | 未知协议明确标记原因 |
| 订阅格式 | 常见 URI 列表、适用的 Clash YAML | 供应商私有字段 | 任意脚本或可执行订阅 |
| 内核 | 插件自管 Mihomo 1.19.31 | sing-box、Xray 等后续适配器 | 外部共享或无法核对的内核 |
| 部署 | 插件私有目录、回环监听、随机密钥 | 离线制品安装 | 公网控制端口或用户指定外部服务 |
| 制品 | 官方固定 URL 与 SHA-256、离线上传同摘要 | 后续版本升级清单 | `latest`、第三方未校验镜像 |

无内核时管理页、订阅解析、节点整理、代理组和规则组编辑仍可使用。原生协议测速、自动组选优、应用配置及实际出口验证会显示缺少内核或统一代理入口的具体原因。

## 全部节点不可用

自动组不会自动加入 `DIRECT`。`fail-closed` 组在全部成员不可用时保持失败，不绕过代理；`keep-last` 由内核保留最后选择，但该节点仍可能无法连接。需要直连时应建立明确的直连规则。
