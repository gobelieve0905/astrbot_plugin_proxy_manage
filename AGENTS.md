# 代理管理中心开发约束

## 规范来源

- 修改本仓库前，必须完整阅读 `docs/PRODUCT_DEFINITION.md`。
- 该文档定义最终产品形态，是产品边界、架构和验收标准的最高优先级依据。当前实现与其不一致时，应把差异视为待迁移事项，不得反向缩小产品目标。
- 若需求确实改变流量范围、内核抽象、安全模型、失败策略或最终验收标准，必须先获得产品所有者明确确认，再更新产品定义并在 CHANGELOG 中说明，然后修改实现。不得仅为迁就现有代码、降低工作量或适配单一内核而降低目标。

## 不可违反的边界

- 插件管理的是 AstrBot 及其 Agent 的实际出站流量，不是单纯管理订阅和节点列表。
- 插件必须自行准备、校验、运行和监督代理内核；普通用户不需要预装或手工配置 Mihomo、sing-box、Xray 等服务。
- 内核必须通过统一适配器接入。业务模型、持久化格式和普通用户界面不得与 Mihomo 或任何单一内核绑定。
- AstrBot 使用稳定的插件入口；机器人平台、Provider、Agent 工具、插件、下载和相关 MCP 外部请求都必须纳入流量接入清单。
- 外部 `DIRECT` 是明确的受管理规则，不得借助 `no_proxy` 或失败回退形成静默旁路。
- 只有真实流量验证通过才能标记“已接管”；配置生成成功、控制 API 可达或节点列表可见都不等于接管成功。
- 尚未实现或无法证明的能力必须显示为规划中、未接入或无法判定，不得在页面和文档中提前宣称完成。

## 每轮修改的完成条件

提交前必须说明并验证：

1. 本轮改善了哪类 AstrBot 实际流量；
2. 影响了哪些规范化模型、内核适配器和流量接入点；
3. 内核专用逻辑是否严格留在适配器内；
4. 如何证明实际请求经过预期出口；
5. 失败关闭、持久化和回滚如何工作；
6. 自动化检查和服务器 AstrBot 验收覆盖了什么；
7. README、用户指南、CHANGELOG 和页面是否准确区分当前能力与最终目标。

若某一项暂不适用，必须明确记录原因，不能静默跳过。
