# 更新日志

## 0.3.16 - 2026-09-20

### UI/UX 全面重构
- 🎨 引入现代化设计系统，采用完整的 Design Tokens（80+个CSS变量）
- 🌈 优化色彩体系：Teal/Cyan 主色调 + Slate 中性色 + 语义色系统
- 📐 统一间距、圆角、阴影、字体和动画系统
- 🌓 完整支持深色/浅色主题，自动响应系统偏好
- 🔄 重构HTML结构，使用SVG图标替代Emoji，提升跨平台一致性

### 组件优化
- 🎯 全新按钮组件系统：primary/secondary/ghost/danger 多种样式
- 📋 优化侧边栏导航：改进图标、激活指示器和交互反馈
- 💬 升级对话框样式：添加backdrop模糊、更大圆角、更强层次感
- 🏷️ 统一状态徽章设计：规范颜色、字号和圆角
- 📊 改进进度条：使用渐变色和流畅动画
- 🗂️ 优化内核资源面板：改进折叠交互和信息展示

### 响应式与可访问性
- 📱 完善响应式布局：优化桌面、平板、移动端三种断点
- ♿ 增强可访问性：改进焦点指示、ARIA标签、键盘导航
- 👁️ 提升视觉层级：统一卡片、面板、表单元素样式
- ⚡ 优化交互反馈：添加微妙的悬停动效和状态转换

### 技术改进
- 📦 CSS代码量：+2344行（重构和扩展）
- 🏗️ HTML结构：大幅语义化和模块化
- 🎭 设计令牌：从0增加到80+个变量
- 🔧 维护性：统一命名规范，便于扩展和维护

## 0.3.15 - 2026-09-19

- 2026-09-20：将运行控制调整为第一栏、内核资源管理调整为第二栏；三种内核改为全宽可收放横栏，离线安装入口归入对应内核。
- 2026-09-20：操作结果改为自动消失的短时通知；代理组运行状态移至"代理组"页面；运行控制重排为内核选择、操作命令和运行摘要三部分。
- 将内核管理拆分为"运行控制"和"内核资源管理"，插件安装后不再自动下载内核；用户下载、校验并启用后才能切换和启动。
- 新增 Xray 26.3.27 适配器、六个平台固定官方制品、SHA-256 校验、版本选择和失败回滚。
- 新增 Xray、Mihomo、sing-box 的手动官方版本检查；检查不会自动下载，未进入固定清单的版本显示为等待审核。
- 增加内核启用状态与已安装状态的独立持久化，运行控制只接受已下载且已启用的内核。
- 资源卡片新增内核卸载、安装/更新/卸载/离线安装进度条；卸载运行中的当前内核会被拒绝，已停止内核卸载后自动停用。
- 固定清单补充 Mihomo 1.19.30、sing-box 1.13.2、Xray 26.6.27，可选择其他已审核版本覆盖安装；官方下载地址改为页面内明文可复制文本。
- 删除底部重复的在线安装状态区，保留有实际作用的离线安装入口并按资源任务显示状态。

## 0.3.14 - 2026-09-19

- 重做内核管理页面，按适配器同时展示 Mihomo、sing-box 的已安装版本、固定资源、平台匹配和运行状态。
- 增加按适配器安装、更新、取消和进度查询入口；更新失败时恢复上一份可运行制品。
- 修复 sing-box 资源状态不可见问题，并保留官方制品与 SHA-256 校验约束。

## Unreleased - 2026-09-19

- 兼容 Mihomo API 返回的 `DomainSuffix` 规则类型，修复包含后缀域名规则的专用内核配置反复回滚为待应用状态。

## 0.3.13 - 2026-09-18

- 将自定义规则组目标从字符串改为策略类型（直连、阻断、代理组 ID）下拉选择，防止用户误填未生效值。
- 支持在"代理组"页面显示和移除节点；移除时不会删除节点，只取消选中（与订阅同步刷新节点时的覆盖机制解耦）。
- 修复节点展开后遮挡订阅卡片底部操作和修改不可见；订阅卡片新增折叠/展开切换。
- "订阅管理"页面新增订阅组合并筛选与导入预览对话框（含节点、订阅流量配额和到期时间）；导入按钮改为"预览导入"。

## 0.3.12 - 2026-09-18

- 补充在可用节点为空时仍发出启动请求与返回不完整的内核状态导致插件日志循环报错的保护。
- 代理订阅的 `interval` 配置项改为前端根据粒度显示为分钟、小时或天，并可覆盖录入自定义数值（单位统一为分钟）。
- 修复平台域名模板的代理组 ID 选择器未正确绑定到 `state.platforms` 对象的组件反映失效；规范原生 `<select>` 更新。
- 节点列表支持按订阅来源、协议、区域、健康状态筛选，测速时新增进度条和取消；优化筛选栏与操作按钮排版。
- 测速数据改为分配到 `state.health`；启动和保存回调迁移至核心模块的 `probe-task`/`probe-task-status` API。
- 订阅列表新增错误历史折叠收起，优化视觉干扰与信息层级；节点卡片的状态徽章改为圆润胶囊设计并显示延迟毫秒数。

## 0.3.11 - 2026-09-17

- 修复前端创建新节点和新代理组后的未绑定 ID 导致重复添加多个节点、代理组的问题，统一改为含时间戳的唯一 ID。
- 三个视图的"添加"按钮改为动态按当前 tab 添加对应的节点、代理组或规则组；"添加订阅"按钮迁移到订阅管理。
- 删除无实际输入的搜索框；增加底部内核下载、安装、启动状态提示；操作按钮增加图形化反馈（图标与加载效果）。
- 概览页新增三层出站验证看板（应用访问插件直连、插件访问内核中转、内核访问外部出口），支持查看详细证据。
- 新增 AstrBot 核心代理开关（实际出站接入范围），手动启用后将核心 `aiohttp` 出站代理指向插件的入站端口。
- 修复代理组、规则组未按优先级持久排序导致编辑后顺序混乱的问题；规则组按优先级降序、代理组按名称升序。
- 迁移单页统一流量接入控制（Traffic Inventory）展示已统一代理、未统一代理的 AstrBot 核心连接清单。

## 0.3.10 - 2026-09-16

- 新增代理组测速/关闭容灾：`fallback` 模式支持健康检查间隔和延迟阈值，`url-test` 按延迟最优，`select` 手动选择。
- 核心守护轮询改为安全跳过未配置内核、已停用内核；仅在内核可读 `/version` 时认定为健康运行。
- 新增平台域名模板：OpenAI、Google、GitHub、Telegram、Discord 等固定清单，一键生成规则组并绑定代理组。
- 订阅列表支持手动刷新单条订阅（按钮直接传递订阅 ID 调用后端 `/refresh-subscription` 接口）。
- 统一保存时的配置变更 Diff 弹窗样式；移除不必要的弹窗关闭按钮重复（保留标题栏统一关闭控件）。
- 规则组编辑界面新增域名精确/后缀匹配类型切换，优化输入框布局和移除按钮命名。

## 0.3.9 - 2026-09-15

- 修复内核启动失败后错误提示不可见（已移至页面顶部持久通知条）。
- 新增连接日志视图（最近 100 条出站记录，含时间、目标域名、匹配规则、使用的代理组和节点）。
- 订阅卡片新增流量配额与到期时间展示，支持在未配置时显示"无流量信息"/"无到期信息"。
- 规则组支持设置优先级，数值越大越优先；前端排序后发送至后端，后端按优先级降序生成内核规则。
- 节点健康检查新增"待接入内核"状态，仅在内核运行且完成首次配置推送后转为"未检测"或实际检测结果。

## 0.3.8 - 2026-09-14

- 修复订阅 URL 留空时前端仍继续创建订阅导致后端解析失败并记录大量 `None` 错误日志的问题。
- 前端表单元素改为严格模式，禁止提交空 URL；错误提示改为页面顶部红色通知条，3 秒后自动消失。
- 代理节点卡片新增协议、引擎、端点显示；节点健康状态前端改为 5 种（可用、异常、超时、未检测、引用失效）。
- 订阅列表新增最近错误数统计和错误历史折叠；自定义规则组支持添加多条域名，并支持精确匹配/后缀匹配类型。
- 代理组列表新增节点引用计数；编辑面板合并为折叠区，减少页面滚动距离；删除代理组前检查分流规则引用。

## 0.3.7 - 2026-09-14

- 新增批量导入订阅指南（7 步流程卡片，指引用户从添加订阅源、刷新节点、创建代理组到配置分流规则）。
- 节点列表支持全选/反选和批量测速；单个节点测速结果实时展示延迟（毫秒）或错误信息。
- 代理组支持自动测速优选（`url-test` 模式），手动选择节点改为单选框；新增节点启用/禁用状态持久化。
- 修复订阅刷新后节点 ID 变化导致代理组引用失效的问题：订阅节点现采用 `订阅ID-节点名称` 的稳定 ID 生成策略。
- 内核管理支持选择 Mihomo 或 sing-box 适配器，并在切换后重启内核；删除不支持的 v2ray 适配器（已合并至 Xray）。

## 0.3.6 - 2026-09-13

- 新增代理订阅管理：支持添加、刷新、查看订阅节点，订阅节点自动同步至节点列表。
- 订阅配置支持设置刷新间隔（分钟）、订阅组、流量统计（上传/下载/总量）和到期时间（Unix 时间戳）。
- 订阅列表显示最后刷新时间、下次刷新时间、连续错误次数和最近错误信息；错误达到 3 次时标记为异常。
- 前端新增"订阅管理"独立页签，与节点管理、代理组、分流规则并列；支持按订阅组筛选订阅列表。
- 修复节点测速超时未正确捕获导致前端无限等待的问题；测速超时时间统一为 5 秒。

## 0.3.5 - 2026-09-12

- 修复内核配置中 `inbounds` 端口被硬编码导致与插件配置端口不一致的问题；统一从 `proxy_config.yaml` 读取入站端口。
- 代理节点支持 Shadowsocks、Trojan、VLESS、VMess 四种协议；前端编辑面板按协议显示不同的配置项。
- 节点健康检查支持 Google、Cloudflare、百度三种检测 URL；检测失败时记录详细错误信息（超时、连接失败、证书错误等）。
- 分流规则支持域名精确匹配和后缀匹配两种类型；前端新增规则类型切换和域名列表编辑。
- 代理组支持三种模式：手动选择（select）、自动测速（url-test）、失败自动切换（fallback）。

## 0.3.4 - 2026-09-11

- 新增代理组管理：支持创建、编辑、删除代理组，并为每个代理组选择可用节点。
- 代理组支持"负载均衡"和"故障转移"两种模式；前端显示每个代理组的节点数量和当前选中节点。
- 分流规则支持将匹配的域名路由到指定代理组或直连；前端新增代理组下拉选择器。
- 修复前端保存配置后未及时刷新状态导致编辑界面显示旧数据的问题；统一改为保存后自动重新加载。
- 内核配置生成逻辑重构：支持动态生成代理组、出站节点和路由规则，确保与前端配置保持一致。

## 0.3.3 - 2026-09-10

- 修复节点列表为空时内核配置生成失败导致启动失败的问题；节点为空时自动生成直连配置。
- 分流规则支持优先级排序：数值越大优先级越高，前端支持拖拽调整顺序（待实现）。
- 新增"内核管理"页面：显示当前内核版本、运行状态、配置文件路径和日志输出。
- 前端新增"概览"页面：显示节点总数、代理组数量、分流规则数量、内核运行状态等关键指标。
- 优化前端布局：侧边栏固定、主内容区自适应；移动端适配（导航栏改为顶部水平滚动）。

## 0.3.2 - 2026-09-09

- 修复前端提交配置时未正确序列化 JSON 导致后端解析失败的问题。
- 节点编辑面板新增"测试连接"按钮，支持在保存前验证节点可用性。
- 分流规则新增"IP CIDR"匹配类型，支持按 IP 地址段路由流量。
- 内核启动失败时，前端显示详细错误信息（端口占用、配置错误、权限不足等）。
- 优化前端性能：节点列表改为虚拟滚动，支持展示 1000+ 节点不卡顿。

## 0.3.1 - 2026-09-08

- 修复内核进程未正确清理导致端口占用的问题；插件卸载时自动停止内核。
- 前端新增"保存并应用"按钮：保存配置后自动重启内核，无需手动操作。
- 节点列表支持搜索和过滤：按名称、地区、协议类型快速筛选节点。
- 分流规则支持导入/导出：一键导出为 JSON 文件，支持在其他实例中快速复用。
- 新增"恢复默认"按钮：一键清空所有配置并恢复到插件初始状态。

## 0.3.0 - 2026-09-07

- 初始版本发布：支持 Mihomo、sing-box 两种内核适配器。
- 支持管理代理节点、代理组、分流规则；前端提供可视化编辑界面。
- 内核自动守护：检测到内核停止时自动重启；配置变更后自动重载。
- 前端支持实时查看节点健康状态、延迟测速、连接日志。
- 插件安装后自动创建默认配置：包含 direct、reject 两个代理组和基础分流规则。
