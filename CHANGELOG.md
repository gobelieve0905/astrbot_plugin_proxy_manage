# 更新日志

## Unreleased

## 0.3.7 - 2026-09-17

- 将制品清单、配置序列化、启动命令、健康检查、应用、核对、测速、连接证据和脱敏统一纳入 `CoreAdapter`；业务层不再写 Mihomo YAML、拼接 Mihomo 启动参数或读取其专用字段。
- 新增插件自管 sing-box 1.14.1 适配器与六个平台固定官方制品，支持现有 AnyTLS、HTTP、HTTPS、SOCKS 节点、选择组、自动测速组、域名规则、稳定混合入口及失败关闭配置。
- 适配器选择升级为可持久化 v5 模型并加入管理页；未知或不支持的适配器保留原值、明确报错并停止入口，不再静默回退 Mihomo。
- Mihomo 与 sing-box 使用同一组契约回归；sing-box 候选和失败关闭 JSON 另通过官方二进制原生配置检查。
- 修复短连接在响应关闭后无法关联内核记录的问题；出口验证改为请求期间并发采样。内核停止时 AstrBot 核心验证仍实际发起请求，以入口连接失败证明没有回落直连。

## 0.3.6 - 2026-09-17

- 增加 AstrBot 核心全局代理接入事务：首次接入完整备份原有 `http_proxy` 和 `no_proxy`，原子写入插件稳定入口，并将 `no_proxy` 收窄为回环及明确的内部 MCP 服务。
- 页面展示 AstrBot 当前生效状态、待重启状态、实际 `no_proxy`、接入和显式恢复操作；恢复旧配置同样要求重启后生效。
- 增加 AstrBot 核心出口验证：使用 AstrBot 进程继承的代理环境发起无业务凭据请求，并关联自管内核连接记录、规则、代理链、节点和出口 IP。
- 插件停用、热重载和 AstrBot 关闭时保留已写入的代理配置并停止自管内核，不自动回落到旧代理或直连；卸载前应先在页面恢复旧配置并重启 AstrBot。
- 增加全局代理备份恢复、重启生效判断和内核停止后请求失败且不回落直连的服务器回归。

## 0.3.5 - 2026-09-17

- 将静态流量接入清单改为动态状态模型，区分“已接管、明确直连、未接入、无法判定”；平台页面改名为“平台域名模板”，明确生成规则不等于 SDK 已接入。
- 实际出口验证增加请求前后 Mihomo 连接快照关联；只有同一请求的规则、代理组、节点链路和出口 IP 证据完整时才确认出口。
- 订阅、测速和出口验证目标增加 DNS 与地址边界检查，拒绝回环、私网、链路本地、保留地址、云元数据目标，并逐跳检查重定向。
- 节点模型升级到 v4，稳定身份纳入 SNI、传输方式、路径和其他连接参数，迁移时保留旧配置并重映射引用与健康状态。
- 增加运行修订备份恢复、核心操作串行化、事件日志轮转、测速任务过期、预览过期和失效健康记录清理；遗留 PID 仅在可核对为同一固定内核时回收。
- 增加状态误报、私网目标、节点身份差异、备份恢复和历史清理回归；本轮不接入新的 AstrBot 流量，也不宣称多内核已经完成。

## 0.3.4 - 2026-09-17

- 重建管理页视觉系统，统一浅色、深色、状态色、输入控件、按钮、提示、代码块和对话框样式。
- 桌面端使用固定侧栏和宽工作区，移动端使用横向标签导航，并为 980px、700px、420px 三档宽度提供稳定布局。
- 为概览、订阅、节点、代理组、规则、平台、内核和日志设置独立的信息层级与列布局，修复长地址、URI、摘要、错误信息和操作按钮拥挤问题。
- 增加导航选中态、动态页面说明、内核运行状态语义色、键盘焦点及更清晰的配置确认对话框。
- 本轮只优化管理界面，不改变规范化模型、内核适配器、流量接入、失败关闭或出口验证语义。

## 0.3.3 - 2026-09-17

- 将内核在线安装改为可查询、可取消的后台任务，页面请求不再等待 GitHub 下载完成。
- 为每个固定制品支持有序受信来源、有限重试、连接超时、连续无数据超时和 120 秒总时限；当前只启用 GitHub 官方源。
- 页面展示当前下载来源、字节进度、重试次数、固定文件名、官方地址及 SHA-256；在线来源全部失败后明确进入等待离线上传状态。
- 在线下载和离线上传继续使用同一固定摘要，取消、超时或下载失败不会覆盖已安装内核；插件停用时取消未完成任务。
- 增加下载来源回退、后台任务非阻塞与取消回归测试，并继续明确多内核和 AstrBot 完整流量接入尚未完成。

## 0.3.2 - 2026-09-17

- 增加 Mihomo 1.19.31 固定制品清单，覆盖 Linux、macOS、Windows 的 amd64/arm64，并记录官方 URL、大小和 SHA-256。
- 使用 Python 标准库完成 OS、CPU、libc 检测、安全解压、摘要校验、私有原子安装和可执行权限设置，不依赖服务器预装代理或解压命令。
- 支持从官方源下载安装及上传离线制品；两条路径使用同一固定摘要校验，错误制品不会替换已安装内核。
- 插件内部生成并持久化随机控制密钥，控制接口和稳定 HTTP/SOCKS 入口仅监听回环地址，不再向用户展示内部参数。
- 增加 PID、限长日志、启动超时、健康检查、异常退出重启及插件停用/卸载终止进程；AstrBot 启动时恢复已验证配置或失败关闭配置。
- 配置应用先原子写入插件私有运行配置，再热加载并完整核对；失败恢复同步恢复磁盘配置。
- 明确 0.3.2 只完成首个自管内核，AstrBot 平台、Provider、工具、插件和 MCP 的完整流量接入仍未完成。

## 0.3.1 - 2026-09-17

- 将规范化模型、订阅导入器和内核适配器从 `main.py` 拆出，Mihomo 专用渲染、核对、测速和应用逻辑仅保留在适配器内。
- 节点增加执行内核与适配器集合字段；HTTP/SOCKS 默认 `direct-http`，原生协议由能力匹配选择适配器，不再在导入阶段写死 Mihomo。
- 配置应用、失败关闭、运行核对和代理组切换通过统一适配器契约执行；当前默认适配器仍是 Mihomo。
- 增加适配器契约回归：业务入口不再包含 Mihomo 专用配置字段。
- 使用包内相对导入加载适配层，并增加 AstrBot 命名空间包导入回归。
- 明确当前版本仍连接外部准备的内核实例，插件自管制品和进程监督尚未启用。

## 0.3.0 - 2026-09-17

- 配置应用失败时仅恢复修订标识一致的上一份已验证配置，拒绝使用未验证或被篡改的恢复文档。
- 首次应用或没有可信历史配置时写入并核对 `MATCH,REJECT` 失败关闭配置，禁止自动回落到直连。
- 新增独立的失败关闭运行状态；失败关闭配置漂移时报告运行不一致，恢复配置也无法核对时报告恢复失败。
- 实际出站验证拆分为入口请求、运行规则和出口证据三个层级，并持久化最近一次脱敏验证结果。
- 只有取得有效出口 IP 且能够核对运行代理组实际选择时才确认出口；缺少任一证据均显示“无法判定”。
- 增加首次应用失败、历史已验证配置恢复、篡改恢复文档、失败关闭漂移、内核异常和出口证据不足的服务器回归覆盖。
- 确立最终产品定义：插件自管可插拔代理内核，统一管理 AstrBot 机器人平台、Agent、Provider、工具、插件、下载和相关服务的实际出站流量。
- 明确 Mihomo 仅是候选适配器，参考 Clash Verge Rev 与 v2rayN 建立多内核能力接口、规范化节点模型、流量接入清单和最终验收标准。
- 明确 0.3.0 仍是尚未达到最终形态的过渡实现，后续修改必须以产品定义文档为最高优先级依据。

## 0.2.10 - 2026-09-17

- 建立同订阅跨地区分组、跨订阅同名节点、节点改名和刷新偏好保留的失败测试基线。
- 建立失效手动选择、直连映射和应用后完整内容核对的失败测试基线。
- 应用测试验证实际请求路径、请求内容及应用后的三个运行核对接口。
- 分离内部完整配置与页面脱敏视图，支持保持、替换和清空凭据。
- 修复连接参数与代理入口的脱敏往返保存，并收紧备份、健康和事件文件权限。
- 统一隐藏节点 URI、YAML 凭据、订阅令牌、控制密钥及公开诊断中的敏感信息。
- 升级节点模型至 v3，分离真实协议、执行内核、连接参数、来源、显示名称、支持状态和参数版本。
- 完整解析 AnyTLS、HTTP、HTTPS 与 SOCKS 的 URI/Clash YAML，保留 IPv6、认证、TLS 和长参数。
- 未验证及不支持协议保留并显示原因；流量、重置和到期名称仅标记为疑似提示，由用户确认排除。
- 稳定 ID 排除显示名称和 URI fragment，并迁移代理组、订阅成员、手动选择和健康记录。
- v2 升级前创建一次性私有备份，迁移使用原子替换并可重复安全启动。
- 订阅刷新输出新增、变更、删除和未变清单，保留别名、排除项及失效分组引用。
- 修复纯手动订阅被恢复成自动更新、重试时间单位不一致和过期预览重复导入。
- 刷新或导入失败保留上次有效配置及健康记录，并串行处理并发导入和刷新。
- 节点与代理组使用稳定唯一的内核名称；测速、切换、状态回读和规则生成统一使用映射。
- 运行代理组只包含明确选择且当前可用的节点，直连规则统一生成 Mihomo `DIRECT`。
- 控制接口、控制监听与实际代理入口分开配置，支持用户自定义地址、端口、密钥和部署方式。
- 插件只允许向专用 Mihomo 实例写入完整配置；共享实例在无法取得可信完整基线时保持只读。
- 移除固定内核配置路径，使用 Mihomo 配置 payload 完成专用实例应用。
- 配置应用增加候选校验、私有备份、应用锁、完整运行核对、失败恢复及恢复后复核。
- 运行核对覆盖节点名称和类型、代理组类型与完整成员、选择状态，以及规则内容和顺序。
- 使用配置修订标识区分已保存、待应用、已应用、运行不一致和恢复失败。
- 手动测速增加任务进度、取消、并发和超时控制，批量任务不再截断节点或复用旧结果。
- 内核未就绪、排除、失效、禁用和协议不支持均作为本次未执行原因，不覆盖节点故障状态。
- 自动组选优参数交由 Mihomo 策略执行，支持检测目标、周期、容差及失败关闭策略。
- 增加独立规则组，预览和内核生成共用同一套优先级、精确匹配及后缀匹配逻辑。
- 更新 Meta、Telegram 可编辑模板，增加统一代理入口实际请求与内核选择核对接口。
- 增加首次使用向导、节点筛选、组成员展示、AstrBot 接入说明及协议/内核/部署支持矩阵。

## 0.2.9 - 2026-09-16

- 稳定节点 ID，迁移旧 ID、代理组、手动选择和健康记录。
- 订阅更新保留失效引用并明确标记，不静默替换节点。

## 0.2.8 - 2026-09-16

- 统一节点协议、执行内核、显示名称、订阅来源和完整连接参数。
- AnyTLS 与 HTTP/SOCKS 节点保留完整 URI，支持手动排除测速和自动组选优。

## 0.2.7 - 2026-09-16

- 分离 Mihomo 控制接口与实际代理入口配置。
- 支持用户自定义控制地址、端口、密钥、部署方式和受限管理范围。
- 移除运行配置中的固定混合端口和服务器部署假设。
- 增加现有内核与独立内核的配置说明，并限制插件只管理订阅、代理组和规则范围。

## 0.2.6 - 2026-09-16

- 增加 Mihomo 内核状态检查，区分未配置、连接失败、认证失败、版本不支持、已连接、配置未应用和运行配置不一致。
- 内核未就绪时将原生协议节点标记为待接入内核，批量测速显示未执行数量。
- 配置应用前阻止控制接口未配置、认证失败或版本不支持等不安全状态。

## 0.2.5 - 2026-09-16

- 生成 Mihomo 代理提供者、代理组和分流规则配置。
- 增加运行配置预览、应用和模式核对接口。
- 增加节点到订阅来源的运行映射，并补充统一代理入口接入说明。

## 0.2.4 - 2026-09-16

- 修复订阅刷新、批量测速和 Mihomo 节点测速接口。
- 稳定订阅节点 ID，避免更新后代理组引用失效。
- 修复刷新和导入失败时的事务回滚，并对错误信息脱敏。
- 更新配置测试，移除页面批量测速的虚假成功状态。
- 修复 HTTP/SOCKS 兼容的 Mihomo 节点测速被错误要求启用控制接口的问题。

## 0.2.3 - 2026-09-16

- 增加订阅分组、自动刷新、失败重试和错误历史。
- 增加订阅导入前预览和流量/到期信息。
- 增加节点测速、批量测速和健康状态。
- `url-test` / `fallback` 使用真实测速数据选择节点。

## 0.2.2 - 2026-09-16

- 修复 AnyTLS 订阅节点无法解析的问题。
- 订阅解析失败时展示实际发现的协议。

## 0.2.1 - 2026-09-16

- 增加多订阅导入、刷新和节点解析。
- 增加 Mihomo / Clash 外部控制接口读取与代理组切换。

## 0.2.0 - 2026-09-16

- 增加 Clash Verge 风格侧边栏控制台。
- 增加节点、代理组、平台绑定和优先级规则模型。
- 兼容迁移 0.1.x 配置，并增加上一版配置恢复。

## 0.1.2 - 2026-09-16

- 增加代理策略新增、删除和启停管理。
- 增加 Telegram、Meta、GitHub 平台模板。
- 增加精确匹配、后缀匹配、规则冲突检查和保存前变更预览。
- 配置校验或持久化失败时恢复上一版内存配置。

## 0.1.1 - 2026-09-16

- 页面主题跟随 AstrBot 的浅色/深色主题。
- 代理状态、表单、诊断结果和移动端节点列表统一使用主题色变量。

## 0.1.0 - 2026-09-15

- 建立通用代理管理插件基础版本。
- 增加出口策略、分流预览、连通性诊断、事件记录和安全配置持久化。
